import { Box } from '@material-ui/core';
import React from 'react';
import Form from '../builder/Form';
import config{{pascalcase name}} from './FormConfig';

export default function {{pascalcase name}}Form({ handleSubmit, data, handleCancel }) {
  return (
    <Box p={2} m={1}>
      <Form config={ config{{pascalcase name}} } data={data} onSubmit={handleSubmit} onCancel={handleCancel} />
    </Box>
  );
}
