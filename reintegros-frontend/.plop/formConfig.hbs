import { {{upperCase name}} } from '../../labels';
import { FormConfig } from '../builder/FormConfig';
import { FieldType } from '../builder/FormField';
import { FormData{{pascalCase name}} } from './types';

const config{{pascalCase name}}: FormConfig<FormData{{pascalCase name}}> = [
    //form fields here
   {
    name: 'example',
    type: FieldType.int,
    label: {{upperCase name}}.fields['example'],
    styling: {
      columns: 4,
    },
    rules: {
      required: true,
    },
  },
    ]

export default config{{pascalCase name}};